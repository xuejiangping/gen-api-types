{
	"version": "2.0.0",
	"tasks": [
		{
			"label": "default test",
			"type": "shell",
			"command": ["tsx", "${cwd}/dist/gen_api_types.min.js", "${cwd}/test"],
			"detail": "测试打包后的cli",
			"problemMatcher": ["$tsc"],
			"group": {
				"kind": "test",
				"isDefault": true
			}
		}
	]
}
